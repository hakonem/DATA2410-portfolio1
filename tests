import time
import sys

"""
chunk = '0'*1000

start_time = time.time()
duration = 5
            
while True:
    print(chunk)
    current_time = time.time()
    elapsed_time = current_time-start_time
    
    if elapsed_time > duration:

        print('finished iterating in',str(elapsed_time),'seconds')
        break


tid = 5
duration = time.time() + tid
            
while time.time() < duration:
    sys.stdout.write('0')
    
print('finished!')
print('time is',tid)


str = 'i would go out tonight, but i haven\'t got a stitch to wear'
chunks = [str[i:i+7] for i in range(0, len(str), 7)]
print(chunks)
"""

def print_msg(string,*args):
    print('-' * (len(string)+len(str(arg))))
    print(str)
    print('-' * (len(string)+len(str(arg))))


str = 'Connecting to port'
port = 8188
print_msg(str,port)